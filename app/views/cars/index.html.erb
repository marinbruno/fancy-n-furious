<h1>All fancy cars in here!</h1>

<% @cars.each do |car| %>
  <li>
    <%= car.model %>
    <%= link_to 'See details', car_path(car.id) %>
  </li>
<% end %>

<%= link_to 'Create', new_car_path %>

<h5><%= link_to 'Home', root_path %></h5>
